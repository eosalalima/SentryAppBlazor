services:
  sentryappblazor:
    image: ${DOCKER_REGISTRY-}sentryappblazor
    build:
      context: SentryAppBlazor
      dockerfile: Dockerfile
    volumes:
      - sentry-data-protection-keys:C:\app\data-protection-keys

volumes:
  sentry-data-protection-keys:
